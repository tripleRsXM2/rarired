; Vercel + local installs both need this. @testing-library/react 16.x has
; a peer declaration that strict npm rejects against React 19, even though
; it's actually compatible. legacy-peer-deps mirrors npm 6 behaviour —
; peer deps warn but don't fail the install.
legacy-peer-deps=true
